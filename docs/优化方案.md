# 星途AI 文档体系优化方案

## 一、跨文档一致性问题

### 1.1 六大领域命名不一致（高优先）

04-数据结构化文档 和 07-六大领域定义 已统一为：
`语言沟通 / 社交互动 / 认知学习 / 感觉运动 / 情绪行为 / 生活适应`

但以下文档仍使用旧命名：
- **02-AI能力设计文档** §3.2 children_profiles 字段定义：仍用 `social/communication/cognition/self_care/behavior/play` 六个独立 JSONB 字段
- **02-AI能力设计文档** §2.8 dashboard 一周训练周报：混用旧名（"语言沟通/社交互动/认知学习/感觉运动/情绪行为/生活适应"已正确，但 children_profiles 描述未同步）
- **01-PRD** §3.2：仍写"六领域能力画像（社交、沟通、认知、自理、行为、游戏）"
- **05-前端设计文档** §2.4 卡片清单 RadarChart：描述为"六领域雷达图"但未明确用新名
- **06-前端交互文档** §4.3 RadarChart：domains 字段描述未对齐新领域名

**修复**：统一为 04/07 文档的新命名，02 文档的 §3.2 改为引用 domain_levels JSONB 结构。

### 1.2 children_profiles 表结构不一致（高优先）

- **02-AI能力设计文档** §3.2：描述为 6 个独立 JSONB 字段（social/communication/cognition/self_care/behavior/play），每个含 level/score/trend/sub_items/score_reason
- **04-数据结构化文档**（权威源）：已重构为统一 `domain_levels` JSONB 字段，内部只有 level + updated_at
- **02 文档**还有 score(0-100)、trend、sub_items 等字段，04 文档中没有

**修复**：02 文档 §3.2 需完全对齐 04 文档的 domain_levels 结构，删除 score/trend/sub_items 等已废弃字段。同时需要决定：score 和 trend 是否还需要？如果需要，应在 04 文档中补充。

### 1.3 Edge Function 目录结构不一致

- **CLAUDE.md**：列出 `weekly-report/` 和 `analysis-report/` 两个独立 Edge Function
- **02-AI能力设计文档** §1.1：同样列出这两个
- **01-PRD** §3.3：Edge Function 列表中没有 weekly-report 和 analysis-report
- **02-AI能力设计文档** §2.8：weekly-report 和 analysis-report 的描述放在 dashboard 章节内，但目录结构中又是独立函数

**修复**：01-PRD 的 Edge Function 列表需补充 weekly-report/ 和 analysis-report/。

### 1.4 卡片数量不一致

- **CLAUDE.md**：18 种卡片
- **05-前端设计文档** §2.4：标题写"23 种"，实际列出编号到 23（但跳过了 17/18）
- **06-前端交互文档**：标题写"18 种卡片详细规范"，实际定义了 18 种（含 ScheduleCard）
- **05 文档**新增了 WeeklyReportCard(19)、AnalysisReportCard(20)、TrainingDetailCard(21)、NotificationCard(22)、ScheduleCard(23)

**修复**：统一卡片总数。05 文档的 23 种是最新最全的，CLAUDE.md 和 06 文档需同步更新为 23 种。或者重新编号去掉跳号。

### 1.5 conversations 表在 02 文档中缺失

- **04-数据结构化文档**：已新增 conversations 表（对话会话管理）
- **02-AI能力设计文档** §2.1 orchestrator 对话历史管理：仍只提 chat_messages，未提 conversations 表
- **CLAUDE.md** 数据库表结构索引：未列出 conversations 表

**修复**：02 文档补充 conversations 表的使用说明，CLAUDE.md 更新表清单。

### 1.6 StreakCard 定义位置不一致

- **02-AI能力设计文档** §6.1 看板组件：定义了 streak_card（连续天数卡片）
- **05-前端设计文档** 和 **06-前端交互文档**：卡片清单中没有 StreakCard
- 但 02 文档家长看板组合中使用了 streak_card

**修复**：要么在 05/06 文档中补充 StreakCard 定义，要么将其合并到 SummaryCard 中。

## 二、文档间重复与冗余

### 2.1 角色差异描述重复（中优先）

以下内容在多处重复定义：
- **01-PRD** §3.5：四种角色差异表
- **02-AI能力设计文档** §1.4：四种角色详细定义
- **05-前端设计文档** §1.4 + §3.1-3.4：各角色视觉风格和对话设计
- **CLAUDE.md**：角色差异简表

**建议**：01-PRD 保留简表，02 文档作为角色 prompt 设计的权威源（详细），05 文档只保留视觉差异，其他引用 02 文档。

### 2.2 配色体系重复

- **05-前端设计文档** §1.4 + §3.1-3.4：各角色配色
- **06-前端交互文档** §1.2：完整配色体系（更详细）

**建议**：05 文档的配色简化为引用 06 文档，06 文档作为视觉规范权威源。

### 2.3 看板组件定义重复

- **02-AI能力设计文档** §6：看板组件类型清单 + 角色组合
- **05-前端设计文档** §2.4：卡片类型清单
- **06-前端交互文档** §4：18 种卡片详细规范

**建议**：02 文档只保留"AI 返回什么卡片"的业务逻辑，05 文档保留卡片技术清单，06 文档保留详细视觉规范。

### 2.4 动效规范重复

- **05-前端设计文档** §6：动效规范
- **06-前端交互文档** §6：动效完整规范（更详细）

**建议**：05 文档删除动效章节，引用 06 文档。

### 2.5 AI 虚拟形象重复

- **05-前端设计文档** §4：AI 虚拟形象
- **06-前端交互文档** §2.2 + §7.2：更详细的形象规范

**建议**：05 文档简化为引用 06 文档。

### 2.6 无障碍设计重复

- **05-前端设计文档** §9：无障碍设计
- **06-前端交互文档** §8：无障碍与适配（更详细）

**建议**：05 文档删除，引用 06 文档。

### 2.7 快照协议重复

- **02-AI能力设计文档** §3.4：双层快照协议
- **04-数据结构化文档** §3 child_snapshots：快照表结构和刷新策略

**建议**：02 文档保留快照的业务语义（AI 怎么用），04 文档保留数据结构和刷新映射（技术实现）。

## 三、内容缺失与待补充

### 3.1 03-用户场景文档需同步更新

03 文档中的场景仍引用旧的 Skill Script 概念（虽然更新说明说已移除，但需确认所有场景的数据操作描述是否完全对齐新架构）。

### 3.2 04 文档 SQL 查询示例有字段名错误

- 周报查询用了 `started_at`，但 training_sessions 表定义中是 `session_date` + `created_at`，没有 `started_at`
- 周报查询用了 `domain` 和 `score` 字段，但 training_sessions 表中没有这两个字段
- 分析报告查询用了 `recorded_at`，但 behavior_records 表中是 `occurred_at`
- 分析报告查询 children_profiles 用了 `dimension` 和 `score`，但实际是 `domain_levels` JSONB

### 3.3 02 文档 children_profiles 需要决策

02 文档描述的 children_profiles 有 score(0-100)、trend、sub_items 等丰富字段，但 04 文档只有 level + updated_at。需要明确：
- 如果 score/trend/sub_items 是需要的，04 文档应补充
- 如果已废弃，02 文档应删除

### 3.4 CLAUDE.md 需同步更新

CLAUDE.md 中的数据库表结构描述（"7个域，20张表"）需更新为 04 文档的最新版本（"7个领域域 + 对话域，17张业务表 + 2张对话表 + 3张系统运行表"）。

## 四、优化执行计划

### Phase 1：修复一致性问题（高优先）— ✅ 已完成
1. ✅ 统一六大领域命名（01/02/05/06/CLAUDE.md）
2. ✅ 统一 children_profiles 表结构（02 对齐 04，保留丰富版本 score/trend/sub_items/score_reason）
3. ✅ 统一 Edge Function 列表（01 补充 weekly-report/analysis-report）
4. ✅ 统一卡片数量（CLAUDE.md/06 更新为 23 种）
5. ✅ 补充 conversations 表引用（02/CLAUDE.md）
6. ✅ 修复 04 文档 SQL 查询字段名错误

### Phase 2：消除冗余（中优先）— ✅ 已完成
1. ✅ 05 文档：删除与 06 重复的配色详情/动效/虚拟形象/无障碍章节，改为引用
2. ✅ 02 文档：看板组件章节精简，只保留业务逻辑
3. ⏳ 各文档角色描述去重（低优先，暂不处理）

### Phase 3：内容增强 — ✅ 已完成
1. ⏳ 03 用户场景文档全面检查（待后续迭代）
2. ✅ CLAUDE.md 同步最新架构信息（卡片23种、对话域conversations+chat_messages、Edge Function补充weekly-report/analysis-report）

### 额外完成项
- ✅ 02 文档 Edge Function 根据原型数据字段补充（training-record/behavior-crisis/life-record/weekly-report/analysis-report/training-detail 输出字段 JSON 结构）
