module: assessment
description: "Assessment domain behavior, scoring flow, and evidence traceability."
scope:
  - backend.assessment
workflow:
  required_stages:
    - audit
    - optimize
    - fill_gap
quality_gates:
  required_checks:
    - scoring_logic_verified
    - data_schema_alignment_checked
risk_focus:
  - scoring_drift
  - incomplete_evidence
