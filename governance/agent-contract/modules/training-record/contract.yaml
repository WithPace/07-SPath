module: training-record
description: "Training session record integrity, field completeness, and downstream snapshot consistency."
scope:
  - backend.training_record
workflow:
  required_stages:
    - audit
    - optimize
    - fill_gap
quality_gates:
  required_checks:
    - session_payload_verified
    - session_snapshot_writeback_verified
risk_focus:
  - malformed_session_data
  - snapshot_sync_regression
