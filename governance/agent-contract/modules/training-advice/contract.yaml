module: training-advice
description: "Training advice generation quality, plan feasibility, and recommendation traceability."
scope:
  - backend.training_advice
workflow:
  required_stages:
    - audit
    - optimize
    - fill_gap
quality_gates:
  required_checks:
    - recommendation_quality_verified
    - plan_writeback_verified
risk_focus:
  - low_signal_recommendations
  - plan_feasibility_regression
